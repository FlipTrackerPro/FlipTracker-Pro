# Upgrade Guide

Make a backup copy first. Replace the Apps Script source, run `upgradeFlipTrackerPro()`, refresh, and verify existing Inventory rows remain intact. The document schema version prevents unnecessary migrations.
